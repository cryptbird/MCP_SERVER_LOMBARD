server:
  port: ${PORT:8080}

spring:
  application:
    name: home-insurance-mcp-server

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

app:
  iltc:
    base-url: ${ILTC_BASE_URL:https://api-iltc.insurancearticlez.com/dps-agorax/api/v1/pay}
    username: ${ILTC_USERNAME:ILTakeCare}
    password: ${ILTC_PASSWORD:}
