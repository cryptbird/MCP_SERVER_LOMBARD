server:
  port: ${PORT:8080}

spring:
  application:
    name: home-insurance-mcp-server
  ai:
    mcp:
      server:
        name: icici-lombard-home-insurance
        version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

app:
  iltc:
    base-url: ${ILTC_BASE_URL:https://api-iltc.insurancearticlez.com/dps-agorax/api/v1/pay}
    username: ${ILTC_USERNAME:DIGITAL_WEBSITE}
    password: ${ILTC_PASSWORD:uZKnKo0CEWWmV7jix/EPhg==}
